### env config ###

image: Visual Studio 2015

### build config ###
version: 0.8.1.{build}
pull_requests:
  do_not_increment_build_number: true
configuration: RelWithDebInfo

install:
  - choco install -y innosetup
  - set PATH="C:\Program Files (x86)\Inno Setup 5";%PATH%

build:
  - ps: .\appveyor_build_launcher.ps1
  - ps: .\appveyor_cmake_odamex.ps1
  project: build\Odamex.sln
  verbosity: minimal
before_build:
  - ps: .\appveyor_before_build.ps1
after_build:
  - ps: .\appveyor_after_build.ps1
  - iscc .\installer\windows\odamex.iss /DPWD=%CD% /Obuild

### artifacts config ###

artifacts:
  - path: odamex.zip
  - path: odasrv.zip
